import HomeView from './components/HomeView';
import { UserAuth } from '../../core/helpers';

export default {
  component: HomeView,
  //onEnter: (nextState, replace) => UserAuth.checkAuth(nextState, replace, 1)
}